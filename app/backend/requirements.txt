# Ask the PLC — Stage 2 backend dependencies.
# Install into the existing l5x-copilot venv:
#   ./l5x-copilot/.venv/bin/pip install -r app/backend/requirements.txt
# The parser (src.parser / src.analysis) is imported from ../../l5x-copilot,
# which is added to sys.path at runtime by plc_tools.py — no install needed.

fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.115
mcp>=1.2
python-dotenv>=1.0
httpx>=0.27
websockets>=13
pyyaml>=6.0

# dev / test
pytest>=8.0
pytest-asyncio>=0.24
python-multipart>=0.0.9
claude-agent-sdk>=0.2
